import { SearchconfigPipe } from './searchconfig.pipe';

describe('SearchconfigPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchconfigPipe();
    expect(pipe).toBeTruthy();
  });
});

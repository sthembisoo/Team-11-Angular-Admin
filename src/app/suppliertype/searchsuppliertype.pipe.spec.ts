import { SearchsuppliertypePipe } from './searchsuppliertype.pipe';

describe('SearchsuppliertypePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchsuppliertypePipe();
    expect(pipe).toBeTruthy();
  });
});

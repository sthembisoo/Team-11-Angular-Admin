import { SearchinventoryPipe } from './searchinventory.pipe';

describe('SearchinventoryPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchinventoryPipe();
    expect(pipe).toBeTruthy();
  });
});

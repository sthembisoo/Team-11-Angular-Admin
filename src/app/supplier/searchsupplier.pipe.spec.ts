import { SearchsupplierPipe } from './searchsupplier.pipe';

describe('SearchsupplierPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchsupplierPipe();
    expect(pipe).toBeTruthy();
  });
});

import { SearchcategoryPipe } from './searchcategory.pipe';

describe('SearchcategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchcategoryPipe();
    expect(pipe).toBeTruthy();
  });
});

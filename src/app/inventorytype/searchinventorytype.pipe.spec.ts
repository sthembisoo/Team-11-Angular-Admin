import { SearchinventorytypePipe } from './searchinventorytype.pipe';

describe('SearchinventorytypePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchinventorytypePipe();
    expect(pipe).toBeTruthy();
  });
});
